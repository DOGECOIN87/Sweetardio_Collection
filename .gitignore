output/
__pycache__/
*.pyc
background_pop_studies/grade1_out/
background_pop_studies/samples/compare_*.png
background_pop_studies/GRADE1_GRADE_LOG.md

# dynamic/ proof inputs are regenerated artifacts (python3 dynamic/render.py
# --tokens 3 --variety 6); only the contact SHEETS are the design record.
dynamic/proof/token_*.png
dynamic/proof/var_*.png
dynamic/proof/wx_*.png
dynamic/proof/anim/weather_*_strip.png
dynamic/proof/anim/*.gif

# The promo render is a regenerated artifact of a mint, like output/ — the
# GENERATOR is the record. promo/make_promo.py rebuilds it from output/.
promo/*.mp4
# Lineage artwork is other projects' work, supplied for the intro — not ours
# to redistribute in this repo. make_intro.py renders a labelled placeholder
# for any slot that is empty, so the video builds without them.
promo/lineage/
